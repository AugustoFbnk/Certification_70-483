namespace Certification70_483._04_ImplementDataAccess._02_ConsumeData
{

    /// <summary>
    /// Using a WCF proxy client
    /// Just for exemplify. It doesn't work because don't have a app.config configured to it
    /// </summary>
    public class Example41
    {

        public static void Start()
        {
            //MyServiceClient client = new MyServiceClient();
            //string result = client.DoWork("John", "Doe");
            //Console.WriteLine(result); // Displays JohnDoe
        }
    }
}
